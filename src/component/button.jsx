const Button = ({ title }) => {
    return (
        <span className="mx-1 bg-white p-2 px-4 rounded-full font-bold text-gray-800">
            {title}
        </span>
    );
};

export default Button;